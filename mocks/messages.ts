import { Message, Conversation } from '@/types';

export const mockMessages: Message[] = [
  // No mock messages - users will create their own conversations after signup
];

export const mockConversations: Conversation[] = [
  // No mock conversations - users will create their own conversations after signup
];