import { Cook, Customer } from '@/types';

export const mockCooks: Cook[] = [
  // No mock cooks - users will signup and create their own profiles
];

export const mockCustomers: Customer[] = [
  // No mock customers - users will signup and create their own profiles
];